# Cards — Legacy (미사용)

런타임 import 경로에 연결되지 않은 파일 목록.
삭제 전 참고용으로 보관한다.

| 파일 | 원래 위치 | 비고 |
|------|-----------|------|
| ShuffleScene.tsx | `cards/` | 구 셔플 UI, App 미연결 |
| ShufflePage.tsx | `cards/` | CardsPage로 통합됨 |
| ShuffleStage.tsx | `cards/` | CardDrawStage로 통합 |
| ShuffleStatus.tsx | `cards/` | CardsPage STATUS로 대체 |
| TravelCard.tsx | `cards/` | 블루 티켓 레거시, cards 플로우 미사용 |
| useCardStageMetrics.ts | `hooks/` | CardDrawStage useStageSize 사용 |

`design_2.5/`는 초기 참고용 — 구현 기준 아님.
