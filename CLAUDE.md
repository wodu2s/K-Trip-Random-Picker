# CLAUDE.md

- 모든 답변은 한국어로 짧게 작성한다.
- 요청을 다시 설명하거나 장황하게 요약하지 않는다.
- 관련 파일만 읽고 `node_modules`, `dist`, `build`, `.git`은 탐색하지 않는다.
- 먼저 `rg` 또는 파일명 검색으로 범위를 좁힌 뒤 필요한 파일만 연다.
- 한 번에 요청된 작업만 수행하고 임의의 리팩터링·기능 추가를 하지 않는다.
- 기존 구조·기능·자산을 최대한 재사용한다.
- 수정 전 긴 계획은 생략하고 바로 조사·작업한다.
- 확실하지 않으면 추측하지 말고 실제 import·사용 관계를 확인한다.
- 새 파일 생성과 파일 삭제는 꼭 필요한 경우에만 한다.
- 코드 주석·문서·테스트는 요청받았을 때만 추가한다.
- 완료 보고는 `수정 파일 / 핵심 변경 / 검증 결과 / 남은 문제`만 적는다.
- 가능한 경우 관련 검사나 `npm run build`만 실행하고 성공 여부만 보고한다.
